{% extends 'base2.html.twig' %}
{% block body %}
    {#{% if is_granted('ROLE_ADMIN') %}#}
    <h1 class="alert alert-success">Modificar Datos del Usuario</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Modificar'}) }}

    {#{{ include('user/_delete_form.html.twig') }}#}
    {#{% else %}#}
        {#<h2 class="alert alert-danger">Usted no tiene permiso para realizar esa acci&oacute;n!!</h2>#}
    {#{% endif %}#}

{% endblock %}
{% block topnavlink %}
    {% if is_granted("ROLE_ADMIN") %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('user_index') }}"><i class="fa fa-list"></i> Listar</a>
    </li>
    <li class="nav-item">
        {{ include('user/_delete_form.html.twig') }}
    </li>
    {% endif %}
{% endblock %}
