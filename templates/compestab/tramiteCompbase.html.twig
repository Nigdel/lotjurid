{% extends 'base2.html.twig' %}
{% block body %}
    {% import 'compestab/comptramit.html.twig' as tramite %}
<h6 class="alert alert-success">Comprobantes de Instalaciones {{ tramitecomp | default("Pendientes de Impresion") }} <span class="badge badge-warning">{{ comps | length }}</span></h6>
    <table class="table table-striped table-dark table-sm">
        <thead>
        <tr>
            <th>Instalacion</th>
            <th>Folio</th>
            <th>Estado</th>
            <th>F. Emitido</th>
            <th>Duplicado</th>
            <th>Ver/Editar</th>
            <th>Trámites</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in comps %}
            <tr>
                {{ tramite.showcompFila(entity,loop,'true') }}
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}