<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Jekyll v4.0.1">
    <title>{% block title %}Gestion de Licencias de Operacion de Transporte{% endblock %}</title>
    <!--<link rel="canonical" href="https://getbootstrap.com/docs/4.5/examples/dashboard/">-->
    <!-- Bootstrap core CSS -->
    <!--<link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">-->

    <link href="{{ asset('css/fontawesome5/css/all.css') }}" rel="stylesheet">
    <link href="{{ asset('css/select2.min.css') }}" rel="stylesheet"/>
    <link href="{{ asset('css/bootstrap.css') }}" rel="stylesheet">
    <link href="{{ asset('css/bootstrap-datetimepicker.min.css') }}" rel="stylesheet">
    <style>
      .datepicker {
        font-size: 0.875em;
      }
      .datepicker td, .datepicker th {
        width: 1.5em;
        height: 1.5em;
      }
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
    </style>
    <!-- Custom styles for this template -->
    <link href="{{ asset('css/dashboard.css') }}" rel="stylesheet">
    {% block aditionalcss %}{% endblock %}
  </head>
  <body class="theme-dark">
  <div class="loader">
      <H1 class="text-light bg-dark fixed-bottom"> Estamos procesando su petici&oacute;n...</H1>
  </div>
    <nav class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow d-flex  align-items-center navbar-expand-lg navbar-expand-md">
        <a class="navbar-brand col-md-3 col-lg-2 mr-0 px-3" href="{{ path('portada') }} " title="Ir a la portada"> <i class="fa fa-home"></i> LotJurid</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% block topnavlink %}

                {% endblock %}
            </ul>
       </div>
       <div class="form-inline my-1 my-lg-0">
           {#<a href="#" class="nav-link rounded-pill align-self-baseline">#}
               {#<i class="fas fa-bell"></i>#}
               {#<span class="badge badge-danger badge-pill">3</span>#}
           {#</a>#}
           <div class="d-flex bd-highlight rounded-pill align-self-baseline mx-4" style="background-color: #242939;color: #c0c4d1;">
               <div class="p-1 flex-fill bd-highlight nav-link d-inline-block" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                   <a href="#" style="text-decoration: none;color: #cFcFdF;">
                       <span class="d-inline">Hola, {{ app.user.nombreApellidos | default("Usuario") }}</span>
                       <img alt="Pic" src="{{ asset('plantilla/300_25.jpg') }}" style=" align-self: right; max-height: 38px; max-width: 38px; margin:0 1px 0 5px ;
            border-radius: 50%;">
                       {% if app.user.MensajesUnread  %}<span class="badge badge-warning">{{ app.user.MensajesUnread  }}  </span>{% endif %}
                   </a>
               </div>
               <div class="dropdown-menu dropdown-menu-right mt-0 pt-0 pb-0 mr-2" style="box-shadow: 0 0 1rem 0.5rem rgba(0, 0 ,0,0.5);">
                   <div class="list-group">
                       <li class="list-group-item list-group-item-action" style="background-color: #242939;color: #c0c4d1;">
                           {{ app.user.nombreApellidos | default("Usuario") }}
                       </li>
                       <a href="{{ path('user_show', {'id': app.user.id }) }}" class="list-group-item list-group-item-action" style="background-color: #242939;color: #c0c4d1;">
                           <i class="fas fa-user" style="color: #28a745;"></i>
                           Ver mi Perfil</a>
                       <a href="{{ path('user_chgpassw') }}" class="list-group-item list-group-item-action" style="background-color: #242939;color: #c0c4d1;">
                           <i class="fas fa-key" style="color: #5867DD;"></i>
                           Cambiar Password</a>
                       <a href="{{  path('user_msjs') }}" class="list-group-item list-group-item-action" style="background-color: #242939;color: #c0c4d1;">
                           <i class="fas fa-book-reader" style="color: #d4dd67;"></i>
                           Mis Mensages {{ app.user.MensajesUnread ? '+'~app.user.MensajesUnread : '' }} </a>
                       <a class="list-group-item list-group-item-action" href="{{ path('app_logout') }}" style="background-color: #242939;color: #c0c4d1;">
                           <i class="fas fa-sign-out-alt" style="color: #dc3545"></i>
                           Salir</a>
                   </div>
               </div>
           </div>
       </div>

      </nav>
    {% for message in app.flashes('success') %}
    <div aria-live="polite" aria-atomic="true" style="position: relative;" >
      <div class="toast bg-success shadow p-3 mb-3 rounded" style="position: absolute; top: 0; right: 0; z-index: 1030" data-autohide="false">
            <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <div class="toast-body">
                <i class='fa fa-check'></i> {{ message  }}
            </div>       
        </div>
    </div> 
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div aria-live="polite" aria-atomic="true" style="position: relative;">
          <div class="toast bg-danger shadow p-3 mb-3 rounded" style="position: absolute; top: 0; right: 0; z-index: 1030" data-delay="5500">
                <div class="toast-body">
                    <i class='fa fa-times-circle'></i> Error. {{ message }}
                </div>
            </div>
        </div>
    {% endfor %}
    <div class="container-fluid">
    <div class="row">
      {#side bar#}
      {% include 'sideBar.html.twig' %}
       {#modals para buscar #}
      {% include ('utiles/modal.html.twig') with {
          'idmodal':'personasjuridicasModal',
          'idmodalpath':'personasjuridicas_index',
          'idmodalcriterio':'Nombre',
          'idmodaltitle':'Buscar Persona Jurídica',
          'idmodalfieldname':'nombre'
      } %}
        {% include ('utiles/modal.html.twig') with {
            'idmodal':'lotjuridicasModal',
            'idmodalpath':'lotjuridicas_index',
            'idmodalcriterio':'Expediente',
            'idmodaltitle':'Buscar Lot Jurídica',
            'idmodalfieldname':'lot'
        } %}
        {% include ('utiles/modal.html.twig') with {
            'idmodal':'estabModal',
            'idmodalpath':'instalaciones_index',
            'idmodalcriterio':'nombre',
            'idmodaltitle':'Buscar Instalaciones',
            'idmodalfieldname':'nombre'
        } %}
        {% include ('utiles/modal.html.twig') with {
            'idmodal':'MediosModal',
            'idmodalpath':'medios_trans_index',
            'idmodalcriterio':'nombre',
            'idmodaltitle':'Buscar Medio',
            'idmodalfieldname':'nombre'
        } %}
        {% include ('utiles/modal.html.twig') with {
            'idmodal':'CompModal',
            'idmodalpath':'comprobante_index',
            'idmodalcriterio':'folio',
            'idmodaltitle':'Buscar Comprobante',
            'idmodalfieldname':'folio'
        } %}
      <main role="main" class="col-md-9 ml-sm-auto col-lg-10 px-md-4" style="min-height: 80%;">

        {#<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">#}
          {#<div class="btn-toolbar mb-2 mb-md-0">#}
            {#<div class="btn-group mr-2">#}
              {#<button type="button" class="btn btn-sm btn-outline-secondary">Share</button>#}
              {#<button type="button" class="btn btn-sm btn-outline-secondary">Export</button>#}
            {#</div>#}
            {#<button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">#}
              {#<span data-feather="calendar"></span>#}
              {#Esta Semana#}
            {#</button>#}
          {#</div>#}
        {#</div>#}
        <!--Botones-->
        <div style="min-height: 400px">
            {% block body %}
              <!--<canvas class="my-4 w-100" id="myChart" width="900" height="380"></canvas>-->
            {% endblock body %}
        </div>

        <footer class="container" style="text-align: center">
          <hr/>
          <p>&copy; Unidad Estatal de Tr&aacute;fico <strong> {{ date('now') | date('Y') }}</strong></p>

            {% block friendsites %}{% endblock %}


        </footer>
      </main>
    </div>
  </div>



    <script src="{{ asset('js/jquery-3.5.1.min.js') }}" type="text/javascript"></script>
    <script>window.jQuery || document.write('<script src="js/jquery-3.5.1.min.js"><\/script>')</script>
    <script src="{{ asset('js/bootstrap.bundle.js') }}"></script>
    <script src="{{ asset('js/select2.min.js') }}"></script>
    <script src="{{ asset('js/moment.min.js') }}"></script>
    <script src="{{ asset('css/fontawesome5/js/all.js') }}"></script>
    <script src="{{ asset('js/bootstrap-datetimepicker.min.js') }}"></script>
    <script src="{{ asset('js/es.js') }}" type="text/javascript"></script>
    <!--<script src="https://cdnjs.cloudflare.com/ajax/libs/feather-icons/4.9.0/feather.min.js"></script>-->
    <!--<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.7.3/Chart.min.js"></script>-->
    <script src="{{ asset('js/dashboard.js') }}"></script>
    <script type="text/javascript">
        var acc = document.getElementsByClassName("accordion");
        var i;
        for (i = 0; i < acc.length; i++) {
            acc[i].addEventListener("click", function() {
                /* Toggle between adding and removing the "active" class,
                to highlight the button that controls the panel */
                this.classList.toggle("active");

                /* Toggle between hiding and showing the active panel */
                var panel = this.nextElementSibling;
                // if (panel.style.display === "block") {
                //     panel.style.display = "none";
                // } else {
                //     panel.style.display = "block";
                // }
                if (panel.style.maxHeight) {
                    panel.style.maxHeight = null;
                } else {
                    panel.style.maxHeight = panel.scrollHeight + "px";
                }
            });
        }
        $(document).ready(function(){
            $(".toast").toast("show");
            $(".loader").fadeOut("slow");
        });

        //lol();
        $('.mi-selector').select2();
        $('.select2').css('width','100%');
        $('.select2-selection__rendered').addClass('select2-selection__rendered form-control');
        $(function () {
            $('.datetimepicker4').datetimepicker({
                format: 'L',
                locale: 'es' });
        });

    </script>
    <script>
      function load() {
          $(".loader").fadeIn("slow");
      }
      window.DOMContentLoaded  = load;
    </script>
    {% block jsadicional %}

    {% endblock %}
  </body>
</html>
