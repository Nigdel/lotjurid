{% extends 'base2.html.twig' %}

{% block title %}Extension{% endblock %}

{% block body %}
    <h1>Extension</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extension.id }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ extension.Extension }}</td>
            </tr>
            <tr>
                <th>ExtensionAnexo2</th>
                <td>{{ extension.ExtensionAnexo2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extension_index') }}">back to list</a>

    <a href="{{ path('extension_edit', {'id': extension.id}) }}">edit</a>

    {{ include('extension/_delete_form.html.twig') }}
{% endblock %}
