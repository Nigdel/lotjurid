<form method="post" action="{{ path('direccion_nacional_delete', {'id': direccion_nacional.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ direccion_nacional.id) }}">
    <button class="btn">Delete</button>
</form>
