<form method="post" action="{{ path('oficina_mcpal_delete', {'id': oficina_mcpal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oficina_mcpal.id) }}">
    <button class="btn">Delete</button>
</form>
